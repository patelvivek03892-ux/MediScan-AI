'use client';

import React from 'react';

/**
 * Standard professional medical software interface:
 * Native OS cursor ensures maximum responsiveness, zero lag,
 * and compliance with accessibility and WCAG standards.
 */
export const CustomCursor: React.FC = () => {
  return null;
};
